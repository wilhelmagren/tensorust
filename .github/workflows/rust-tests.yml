name: Tests

on:
  push:
    branches: [ main ]
  pull_requests:
    branches: [ main ]

env:
  CARGO_TERM_COLOR: always

jobs:
  tests:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout/@v2
    - name: Tests
      run: cargo test --verbose

